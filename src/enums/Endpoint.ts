export enum Endpoint {
  item = 'item',
  user = 'user',
  topstories = 'topstories',
  newstories = 'newstories', // 500
  beststories = 'beststories', // 200
  maxitem = 'maxitem',
  updates = 'updates',

  // unavail
  bestcomments = 'bestcomments',
}
